# npm configuration file
# This file is also used by pnpm for registry and authentication settings

# Registry settings
registry=https://registry.npmjs.org/

# Authentication (uncomment and configure as needed)
# For private registries or scoped packages
# @your-scope:registry=https://your-private-registry.com/
# //your-private-registry.com/:_authToken=${NPM_TOKEN}

# Security settings
# Audit level for security vulnerabilities
audit-level=moderate

# Automatically fix security issues when possible
fund=false

# Package settings
# Save exact versions (no ^ or ~)
save-exact=true

# Save to devDependencies by default for CLI tools
save-dev=false

# Engine settings
# Strict engine checking
engine-strict=true

# Node version (should match package.json engines)
# node-version=18.18.0

# Cache settings
# Cache location (optional, pnpm has its own cache)
# cache=/path/to/cache

# Progress settings
progress=true

# Logging
loglevel=info

# Network settings
# Timeout for requests
fetch-timeout=60000
fetch-retries=3

# Optional: Proxy settings (uncomment if needed)
# proxy=http://proxy.company.com:8080
# https-proxy=http://proxy.company.com:8080
# no-proxy=localhost,127.0.0.1

# Optional: Certificate settings (uncomment if needed)
# ca=/path/to/certificate.pem
# cert=/path/to/client-cert.pem
# key=/path/to/client-key.pem
